# this_file: src/vexy_dex/__main__.py
"""Entry point: `python -m vexy_dex` and the `vexy-dex` script."""

from __future__ import annotations

from .cli import main

if __name__ == "__main__":
    main()
