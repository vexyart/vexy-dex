# this_file: pyproject.toml
[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "vexy-dex"
dynamic = ["version"]
description = "Turn an HTML page into slide decks (PDF/SVG) through several engines at once."
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Adam Twardoch" }]
dependencies = [
    "httpx[http2]>=0.27",
    "beautifulsoup4>=4.12",
    "lxml>=5.2",
    "selectolax>=0.3.21",
    "pypdf>=5.0",
    "fire>=0.6",
    "loguru>=0.7",
    "rich>=13.7",
    "anyio>=4",
]

[project.optional-dependencies]
# Heavy / platform-specific engines kept optional so the core installs anywhere.
browser = ["playwright>=1.48"]
weasyprint = ["weasyprint>=63"]
extract = ["trafilatura>=1.12"]
pdf = ["pikepdf>=9.4"]
vision = ["instructor>=1"]
all = [
    "playwright>=1.48",
    "weasyprint>=63",
    "trafilatura>=1.12",
    "pikepdf>=9.4",
]

[project.scripts]
vexy-dex = "vexy_dex.__main__:main"

[project.entry-points."vexy_dex.readers"]
static = "vexy_dex.readers.static:StaticReader"
dynamic = "vexy_dex.readers.dynamic:DynamicReader"

[project.entry-points."vexy_dex.importers"]
webflow = "vexy_dex.importers.webflow:WebflowImporter"
mkdocs-material = "vexy_dex.importers.mkdocs:MkdocsImporter"
docusaurus = "vexy_dex.importers.others:DocusaurusImporter"
bubble = "vexy_dex.importers.others:BubbleImporter"
framer = "vexy_dex.importers.others:FramerImporter"
generic = "vexy_dex.importers.generic:GenericImporter"

[project.entry-points."vexy_dex.exporters"]
weasyprint = "vexy_dex.exporters.weasyprint:WeasyPrintExporter"
playwright = "vexy_dex.exporters.playwright:PlaywrightExporter"
vivliostyle = "vexy_dex.exporters.vivliostyle:VivliostyleExporter"
decktape = "vexy_dex.exporters.decktape:DeckTapeExporter"
prince = "vexy_dex.exporters.prince:PrinceExporter"

[dependency-groups]
dev = ["pytest>=8", "pytest-cov>=5", "mypy>=1.11"]

[tool.hatch.version]
source = "vcs"

[tool.hatch.build.hooks.vcs]
# Git-tag-based semver written to a gitignored module at build time.
version-file = "src/vexy_dex/__version__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/vexy_dex"]
artifacts = ["src/vexy_dex/assets/**"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"

[tool.ruff]
target-version = "py312"
line-length = 88
